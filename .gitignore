# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Zig cache
.zig-cache/
zig-out/

# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Databases
# We want to track scientific_intel_v2.db so Render can host it!
# *.db
# *.sqlite3

# Test output
latest_report.html
temp_input.json

# Data output — exclude raw caches but track dedup filters and dashboard
data/digests/
data/*.json
data/*.jsonl
# Bloom filter files (one per subscriber, ~90 KB each, fixed size forever)
!data/dedup/
!data/dedup/*.bloom

# IDEs and Editors
.vscode/
.idea/
*.swp
*.swo
*~
*.db
